{% if breadcrumbs is defined and breadcrumbs|length > 0 %}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="{{ path('app_roleplay') }}">
                <i class="fas fa-home"></i> Accueil
            </a>
        </li>
        
        {% for breadcrumb in breadcrumbs %}
            <li class="breadcrumb-item {{ loop.last ? 'active' : '' }}">
                {% if not loop.last and breadcrumb.path is defined and breadcrumb.path is not null %}
                    <a href="{{ breadcrumb.path }}">
                        {% if breadcrumb.icon is defined %}<i class="{{ breadcrumb.icon }} me-1"></i>{% endif %}
                        {{ breadcrumb.label }}
                    </a>
                {% else %}
                    {% if breadcrumb.icon is defined %}<i class="{{ breadcrumb.icon }} me-1"></i>{% endif %}
                    {{ breadcrumb.label }}
                {% endif %}
            </li>
        {% endfor %}
    </ol>
</nav>
{% endif %}