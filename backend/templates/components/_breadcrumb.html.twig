{% if breadcrumbs is defined and breadcrumbs|length > 0 %}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="{{ path('app_roleplay') }}">
                <i class="fas fa-home"></i> Accueil
            </a>
        </li>
        

    </ol>
</nav>
{% endif %}